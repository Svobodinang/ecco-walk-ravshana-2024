export default class Swipers {
    constructor() {
        this.initSwipers();
    }

    // eslint-disable-next-line
    initSwipers() {
        // * example for 1 swiper
        // this.swiperNav = new Swiper('.swiper-nav', {
        //     modules: [Navigation],
        //     navigation: {
        //         nextEl: '.nav-slide-next',
        //         prevEl: '.nav-slide-prev'
        //     }
        // });

        // * example for array of swiper
        // data.heroes.forEach((hero, index) => {
        //     const swiperEl = document.querySelector(`.swiper-products-${index}`);

        //     this[`swiperProducts${index}`] = new Swiper(swiperEl, {
        //         modules: [Navigation],
        //         navigation: {
        //             nextEl: swiperEl.querySelector('.slide-next'),
        //             prevEl: swiperEl.querySelector('.slide-prev')
        //         }
        //     });
        // });
    }
}
